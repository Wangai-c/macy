- Implement the figma_design image in the folder "ai"
- Write the code inside the "src/Intro.jsx" and "src/Intro.css"
- You can find the assets inside the: "../assets folder"
- The fonts used can be found locally in the system (The ones used are: Chillax Variable and Rubik Doodle Shadow)
- The images have descriptive names (somehow anyway)
- There's an Audio component that already exists in "src/AudioPlayer.jsx" No need to re-implement it, just attach it as is
- Ignore some inconsistencies between the audio component and the design (leave the component as is)
- Group each major block in the design as a separate component in the "src/Intro.jsx". Though the large image can sit in
the parent component as a column. Center-vertical the components in the second column
- Try to use the colors defined in the "src/index.css" theme where you can

Potential problems

- In the design image there's a white frame with a panda cartoon on the left. The actual image has a white background.
- So as a trick the entire frame has a white gradient background where the left (start) is completely white and the end
(right) is about 80% opaque
- There's an emoji used: use the celebration_emoji.svg in the "assets/svgs" folder
- Don't forget to package assets for vite bundling

In case of questions ask
